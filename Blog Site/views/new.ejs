<%-include("layout/header")%>

<div class="ui main container text segment">
    <div class="ui huge header">New Blog</div>
    <form class="ui form" action="/blogs" method="POST">
        <div class="field">
            <label>Title:</label>
            <input name="blog[title]" placeholder="Title."required>
        </div>
        <div class="field">
            <label>Image:</label>
            
            <input name="blog[image]" placeholder="image">
        </div>
        <div class="field">
            <label>Content:</label>
            <textarea name="blog[body]" placeholder="Blog Content"></textarea>
        </div>
        <button class="ui brown big inverted button">
            Create
        </button>
    </form>
</div>

<%-include("layout/footer")%>